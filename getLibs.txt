
my notes...

# installing R packages
http://wiki.umassrc.org/wiki/index.php/Installing_Software_and_Modules
https://stackoverflow.com/questions/11488174/how-to-select-a-cran-mirror-in-r
-- put a new repository on the hard-drive (Rlib here)
-- * module load gcc/5.1.0 first ... need to have gcc loaded because the packages must be compiled
-- module load R/3.4.0 
-- open R and run install.packages('msm', lib='~/COCA_HPCC/Rlib/', repos='http://cran.us.r-project.org')
   (note that you specify the download location as well as the cran mirror (repos) )


=========================================

-- Open HPCC

-- run: module load gcc/5.1.0

-- run: module load R/3.4.0

-- open R by typing R in the console

-- In R, run:

install.packages(c('gmm', 'mvtnorm', 'tmvtnorm', 'expm', 'msm', 'Matrix', 'TMB', 'abind', 'forcats', 'readr', 'tidyverse'), 
                 lib='~/COCA_HPCC/Rlib/', repos='http://cran.us.r-project.org')

(Note that you have to adjust the directory to the one you're using ... I think it was something like
'~HPCC/Rlib' but I'm not sure.  The "~" just indicates your home directory.) 

